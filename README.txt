このアプリは診療科ごとの全身麻酔手術件数を可視化し、グラフ＋サマリーテーブル付きのPDFとして出力できます。

機能：
- グラフ＋数値指標を含むPDFレポート生成
- 全診療科のPDFを一括Zip化して出力

使い方：
1. アプリを実行
2. データ読み込み後、タブ「将来予測」を選択
3. 「PDF出力」または「一括出力（ZIP）」をクリック

必要なPythonパッケージは `requirements.txt` を参照してください。